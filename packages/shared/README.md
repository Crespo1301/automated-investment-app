# Shared Contracts

This folder is reserved for code that must be shared across backend and frontend once the first stable schemas are promoted out of service-local files.

Near-term candidates:

- execution intent schema
- risk rejection reason enum
- strategy configuration model
- broker adapter interface notes

